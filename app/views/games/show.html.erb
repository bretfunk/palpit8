<div class="divider"></div>

<div class="container">
  <div class="row">
    <div class="col s6">
      <h4><%= @game_streams.game_name %></h4>
    </div>
    <div class="col s3 pull-s1">
      <a class="waves-effect waves-light btn" id="follow_button" href="<%= new_follow_path(@game_streams.game_name) %>"><i class="material-icons left">favorite_border</i>Follow</a>
    </div>
  </div>
</div>

<div class="divider"></div><br>

<div class="row">
  <% @game_streams.streams.each do |stream| %>
    <div class="col s3">
      <img src="<%= stream.preview %>" alt="Stream preview"><br>
      <%= stream.stream_name %><br>
      <%= stream.viewers %> viewers on <%= stream.streamer_name %>
    </div>
  <% end %>
</div>

<%= render partial: 'chatrooms/show' %>
